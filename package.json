{
  "name": "stormer-mysql",
  "version": "0.1.1",
  "description": "Implementation of Stormer Store for MySQL",
  "main": "index.js",
  "repository": "git@github.com:Avocarrot/stormer-mysql.git",
  "author": "dmtrs",
  "license": "MIT",
  "engines": {
    "node": ">=6.9.0"
  },
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore .",
    "test": "find ./test -name '*.spec.js' | xargs tape NODE_ENV=test",
    "cov": "find ./test -name '*.spec.js' | xargs istanbul cover tape NODE_ENV=test"
  },
  "devDependencies": {
    "eslint": "^3.15.0",
    "mockdate": "^2.0.1",
    "sinon": "^1.17.7",
    "tape": "^4.6.3"
  },
  "dependencies": {
    "debug": "^2.6.1",
    "mysql": "^2.13.0",
    "stormer": "^0.9.0"
  }
}
